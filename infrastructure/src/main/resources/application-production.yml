amqp:
  host: ${AMQP_RABBITMQ_HOST}
  port: ${AMQP_RABBITMQ_PORT}
  username: ${AMQP_RABBITMQ_USERNAME}
  password: ${AMQP_RABBITMQ_PASSWORD}

mysql:
  username: ${DATABASE_MYSQL_USERNAME:root}
  password: ${DATABASE_MYSQL_PASSWORD:123456}
  schema: adm_videos
  url: ${DATABASE_MYSQL_URL:localhost:3307}

spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none